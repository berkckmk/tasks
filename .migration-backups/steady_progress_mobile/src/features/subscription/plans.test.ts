import assert from 'node:assert/strict'; import test from 'node:test'; import { canCreateAtLimit, planById } from './plans.ts';
test('starter limits fail closed while paid plan limits are unlimited', () => { const starter = planById('starter'); const growth = planById('growth'); assert.equal(canCreateAtLimit(starter.maxActiveHabits, null), false); assert.equal(canCreateAtLimit(starter.maxActiveHabits, 2), true); assert.equal(canCreateAtLimit(starter.maxActiveHabits, 3), false); assert.equal(canCreateAtLimit(growth.maxActiveHabits, null), true); assert.equal(growth.modules.has('goalPlanner'), true); });
