<?xml version="1.0" encoding="utf-8"?>
<!-- The commit button: the one solid accent surface in the widget's whole
     surface area, which is what makes it read as the primary action without
     a size difference. Ink-coloured label, not white — see nocturne_ink. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="12dp" />
    <solid android:color="@color/nocturne_accent" />
</shape>
