<?xml version="1.0" encoding="utf-8"?>
<!--
    Static stand-in shown in the launcher's widget picker.

    It can't be the real widget: the real one is a single ImageView that stays
    empty until the provider renders a bitmap into it, and the picker never
    runs the provider. This approximates the panel with plain views so the
    picker shows something recognisable instead of an empty box.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_vertical"
    android:background="@drawable/widget_preview_panel"
    android:paddingStart="18dp"
    android:paddingEnd="18dp"
    android:paddingTop="14dp"
    android:paddingBottom="14dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/widget_preview_title"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/config_text" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:text="@string/widget_preview_subtitle"
        android:textSize="13sp"
        android:textColor="@color/config_text_muted" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/widget_preview_sections"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="@color/config_accent" />
</LinearLayout>
