<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- Widget setup screen. DeviceDefault rather than the Flutter themes
         above: this is plain framework views launched by the launcher, and
         the system's own text sizing and touch behaviour are the right
         baseline. Everything visual on top of it is Nocturne. The dark
         variant lives in values-night/styles.xml. -->
    <style name="WidgetConfigureTheme" parent="@android:style/Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:windowBackground">@color/config_background</item>
        <item name="android:colorAccent">@color/config_accent</item>
    </style>

    <!--
        The scope picker's theme.

        Transparent and no-title, because the panel has to look like it opened
        out of the widget's own chip rather than like an app screen appearing
        over the home screen. `windowIsFloating=false` matters: a floating
        window is centred and sized by the framework, and this one is anchored
        at a specific offset.
    -->
    <style name="WidgetScopePickerTheme" parent="@android:style/Theme.DeviceDefault.Dialog.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <!--
            **No scrim.** A dim over the whole home screen made a three-row
            popup read as a mode you had entered rather than a menu hanging
            off the chip — and it dimmed the widget the popup belongs to,
            which is the one thing on screen that should stay legible while
            you choose. The quick-add modal keeps its 0.5 dim: that one *is* a
            mode, with a keyboard and a commit step.
        -->
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <!--
        The quick-add modal's theme.

        The scope picker's translucency, with two differences that matter:
        `windowIsFloating=true`, because this panel *is* centred and framework
        sizing is exactly what it wants, and a 0.5 dim — the 50% scrim that
        makes the home screen read as "behind a modal" rather than "still
        interactive".
    -->
    <style name="WidgetQuickAddTheme" parent="@android:style/Theme.DeviceDefault.Dialog.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.5</item>
    </style>

    <!-- Section heading on the setup screen: Nocturne's `h6` kicker. -->
    <style name="ConfigSectionLabel">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">22dp</item>
        <item name="android:layout_marginBottom">9dp</item>
        <item name="android:textSize">11sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:letterSpacing">0.10</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textColor">@color/config_text_muted</item>
    </style>

    <style name="ConfigSectionDescription">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">6dp</item>
        <item name="android:textSize">12.5sp</item>
        <item name="android:lineSpacingMultiplier">1.4</item>
        <item name="android:textColor">@color/config_text_muted</item>
    </style>
</resources>
