import type { DataGateway, Unsubscribe } from '../../core/data/data-gateway.ts'; import { userCollection } from '../../core/data/data-gateway.ts'; import { reportFromDocument, type Report } from './report.ts';
export class ReportRepository { private readonly gateway: DataGateway; private readonly userId: string; constructor(gateway: DataGateway, userId: string) { this.gateway = gateway; this.userId = userId; } watch(onData: (items: Report[]) => void, onError: (error: unknown) => void): Unsubscribe { return this.gateway.watchCollection(userCollection(this.userId, 'reports'), { orderBy: { field: 'createdAt', direction: 'desc' }, limit: 200 }, (rows) => onData(rows.map((row) => reportFromDocument(row.id, row.data))), onError); } }
