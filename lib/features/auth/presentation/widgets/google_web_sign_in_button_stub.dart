import 'package:flutter/widgets.dart';

/// Non-web stub — [renderGoogleWebButton] is only ever called under
/// `if (kIsWeb)` (see google_web_sign_in_button.dart), so this never runs.
Widget renderGoogleWebButton() {
  throw UnsupportedError('renderGoogleWebButton is web-only.');
}
